// Did you know that all these texts are AI-generated?

const buttonTexts = ['Use ✨AI✨ to imagine'];

export function getRandomButtonText() {
	return buttonTexts[Math.floor(Math.random() * buttonTexts.length)];
}
